#include "transfer.h"
#include "big_int.h"

bool transfer::execute_transfer(big_int send_bill_id, big_int receiver_bill_id) {
  //..
}

bool transfer::cancel_transfer(big_int send_bill_id, big_int receiver_bill_id) {
  //..
}